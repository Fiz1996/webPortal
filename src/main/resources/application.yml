spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/supportPortal
    password: 123456
    username: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#mail:
#  protocol: ${MAIL_PROTOCOL:smtp}
#  host: ${MAIL_HOST:smtp.mailtrap.io}
#  port: ${MAIL_PORT:25}
#  username: ${MAIL_USERNAME:d5a82633443820}
#  password: ${MAIL_PASSWORD:483dca5594d56a}

#  mvc:
#    throw-exception-if-no-handler-found: true
#  web:
#    resources:
#      add-mappings: false
server:
  port: 8081

jwt:
  secret: '7iS0FcASEmnnlVvMrq0='
